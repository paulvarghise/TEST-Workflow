# worflow Name
name : First Workflow
# trigger
on :
  push:
  workflow_dispatch:
jobs:
  Hello-job :
  #runner - guthub hosted
    runs-on : ubuntu-latest
    steps:
      - name: hello world
        run : echo "Hello world"
      - name : Even Name
        run : echo "Event Name:${{github.event}}"# context reference
        
  # second job
  echo-goodbye:
      needs : Hello-job
      runs-on : ubuntu-slim
      steps:
        - name: Say GoodBye
          run: echo "Goodbye!"
        # Multiple command
        - name : Multi-line bash
          run : |
            echo "This is"
            echo "Paul here"
            echo "script"
      
